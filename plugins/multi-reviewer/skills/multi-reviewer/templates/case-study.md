---
case_id: round-{{N}}-{{NNN}}-{{key-kebab-case}}
date: {{YYYY-MM-DD}}
round: {{N}}
status: 监视中  # 监视中 / 未闭环 / 修复中 / 已闭环 / 误报作废
severity: {{BLOCKER / HIGH / MEDIUM}}  # 通常只沉淀 MEDIUM 及以上
captured_by: {{单方 reviewer 抓到 / 多方 reviewer 抓到 / 主 agent 复盘}}  # 仅记录"是哪个角色抓到",不区分具体 reviewer 字母(A/B/C/...);保持 N 路对称,见 §0
---

# {{case 一句话标题,例:SQL 字段漂移导致旧接口数据误差}}

## 0. 硬约束(写 case 前必读,完成后删本节)

| 约束 | 为什么 |
|---|---|
| **主体描述 < 500 汉字**(CJK Unified Ideographs,U+4E00-U+9FFF) | 多轮回归时 N reviewer × N 轮 × case 数;case 越短,越能让 reviewer 把注意力留给本轮新发现而不是阅读历史。"主体"指 §1-§5 的中文描述,**不含** frontmatter / §6 状态变更表 / §7 引用片段 / markdown 表格符号。**度量按汉字字符数计**(`wc -m` 在中文按字符算,但会把英文/标点/空白也计入,不准;真要严格用统计命令见下方"写完自检")|
| **不写修复详情 / commit 链接细节** | case 不是修复备忘录;修复留 git log / MR 即可。case 只记"漏点 + 抓法 + 根因 + 抽象规则",§5 / §6 一行带过 |
| **不区分 reviewer**(双盲底线) | 不写"reviewer-A 抓到 / B 没抓到";统一写"reviewer 抓到 / 主 agent 漏掉" |
| **抽象规则不跨项目** | §4 抽象规则只针对本需求 / 本项目;若发现是跨项目通用规律,应升级为 skill reference 修改,而非通过 case 迁移 |

每条 case 的目标读者:**下一轮的 N reviewer**(读 5-10 秒判断本轮要不要重点核)。脱离这个目标的细节都是冗余。

下方各节是写作骨架,**不是必填全部**——能用 1-2 行写清的就 1-2 行,§3 选 1 项足够,§5 §6 各 1 行即可。

## 1. 漏点描述(主 agent 视角)

主 agent 当时的方案 / 自测里**没意识到**:

{{描述漏点。例:
- 我在方案 v1 里假设新接口的 SQL 与老接口字面级等价,理由是"复用同一份 mapper"
- 实际:老 mapper 在 dept_id 为 NULL 时返回全部数据,新接口包了一层非空过滤
- 自测覆盖了 dept_id 非空的主路径,没覆盖 NULL 边界
}}

## 2. reviewer 怎么抓到的

{{描述抓取手法,**用统一语言**,不区分是哪个 reviewer 抓的(双盲对称)。例:
- 用 Grep 比对新老 mapper 的 SQL 字符串差异
- 发现新接口在 ResultMap 之前多了一行 `WHERE dept_id IS NOT NULL`
- 跑 curl 用 NULL 部门数据复现,新接口返回 0 条,老接口返回 380 条
- 严重度判 BLOCKER:数据完整性破坏
}}

## 3. 漏的根因(主 agent 反思)

{{选 1-2 项,补充具体描述:
- [ ] **同源思维**:方案设计与自测覆盖来自同一个上下文,自测无法暴露设计本身的盲区
- [ ] **经验缺口**:本类盲点(NULL 边界 / 跨服务时序 / 并发安全等)主 agent 历史经验少
- [ ] **context 太长**:对话进入第 N 轮后,前期的关键约束被压缩 / 截断遗忘
- [ ] **过度信任老代码**:"复用老接口的 SQL"之类的假设没逐字核对
- [ ] **环境隔离不足**:本地数据规模不足以暴露问题
- [ ] 其它:{{补充}}
}}

## 4. 抽象规则(本需求范围内可迁移)

{{把本次具体盲点提炼成"本需求/本项目下次也要警惕"的规则,**避免**过度抽象成跨需求/跨项目通用规则。例:
- 凡新接口声称"复用老 mapper / 老 SQL",必须**逐字符 diff** SQL 字符串,不能只看 mapper 名
- 凡 dept_id / job_id / position_id 等"层级 ID"字段,必须考虑 NULL 边界
- 凡新过滤条件加在老 ResultMap 之前,必须给跑数据规模差异
}}

## 5. 决策与处置

- **本轮决策**:{{修 / 延后 / 不修(已知风险) / 误报}}
- **决策理由**:{{一句话}}
- **修复路径**(若决定修):{{commit / MR 链接 / 责任人 / 修复轮次目标}}
- **风险接受**(若决定不修):{{接受人 / 接受理由 / 何时复审}}

## 6. 状态变更记录

| 日期 | 轮次 | 状态 | 备注 |
|---|---|---|---|
| {{YYYY-MM-DD}} | {{N}} | 监视中 | 首次记录 |
| {{YYYY-MM-DD}} | {{N+1}} | 修复中 | commit `<sha>` 已修 |
| {{YYYY-MM-DD}} | {{N+2}} | 已闭环 | reviewer 重测无复现,主 agent 复核确认 |

## 7. 下轮 prompt 引用片段(供主 agent 复制)

> 把这个 case 塞进下轮 prompt 时,主 agent 复制下面这一段进 "前轮已知盲点" 段:

```
- `<repo>/.claude/<本日期>/case-studies/round-{{N}}-{{NNN}}-{{key}}.md`(状态:{{当前状态}})
```

> **不要**复制整个 case 内容进 prompt,只放路径,reviewer 自己 Read。

---

## 写作要点(给主 agent 自己看,落盘前删除本段 + §0 硬约束段)

- **核心约束已升到 §0**(<500 汉字 / 不写修复详情 / 不区分 reviewer / 不跨项目),写 case 前先读
- **状态及时更新**:每轮回归后,主 agent 必须更新所有未闭环 case 的状态,而不是只更新本轮新增 case
- **写完自检 — 统计汉字数**:超 500 汉字必须裁,通常 §3 §4 容易膨胀,优先精简这两节
  - PowerShell:`(Select-String -Path <case>.md -Pattern '\p{IsCJKUnifiedIdeographs}' -AllMatches).Matches.Count`
  - Bash:`grep -oE '[一-龥]' <case>.md | wc -l`(基本汉字范围)
  - 整份(含 markdown 符号)估算:`wc -m <case>.md`,但会把英文 / 标点 / 空白都计入,通常比汉字数多 30-50%。**以汉字数为准**
